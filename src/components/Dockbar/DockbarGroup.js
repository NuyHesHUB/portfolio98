import React from 'react';
import styled from 'styled-components';

const StyledDockbarGroup=styled.div`
    
`

const DockbarGroup = () => {
    return (
        <div>
            
        </div>
    );
};

export default DockbarGroup;